## 2026-01-14 - Search Input Pattern
**Learning:** `Input` components in this codebase are raw and lack built-in support for icons or clear buttons.
**Action:** Always wrap `Input` in a `relative` container and position icons absolutely. Use `pl-9` (left icon) and `pr-10` (right button) padding to prevent text overlap.
